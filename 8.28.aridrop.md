为什么, 我的airdrop打不开?


finder侧边栏没有, 设置里面无法挑钩.
现在侧边栏有airdrop了.


但是里面是空的, 看不到我另外的os26的那台, 另外, airdrop里面的电脑的名字在哪里设置? 我去设置一下, 不然现在我都不知道2台studio究竟哪一台有问题.



```sh
killall -9 sharedfilelistd    # 杀掉侧边栏列表后台服务
killall Finder                # 重启访达让它重建

# 图形界面:系统设置 → 通用 → 关于本机 → 点顶部的名称 → 直接改

sudo scutil --set ComputerName "新名字"      # 这是 AirDrop 里别人看到的名字
sudo scutil --set LocalHostName "new-name"   # 这是网络里的 .local 名字


```

