# macOS "最近使用" 不工作的问题排查与修复
# macOS 最近使用和 Spotlight 排查记录

时间：2026-06-01 11:10 左右

## 结论

`mdutil -s /System/Volumes/Data/Users/X` 报：

```sh
/System/Volumes/Data/Users/X:
	Error: unknown indexing state.
```

这个现象本身不是故障。`mdutil` 管的是 Spotlight 的“卷”级索引，不是普通目录索引。`/System/Volumes/Data/Users/X` 和 `/Users/X` 都是目录，所以会报 `unknown indexing state`。

真正该看的状态是 Data 卷：

```sh
mdutil -s /System/Volumes/Data
```

当前实测结果：

```sh
/System/Volumes/Data:
	Indexing enabled.
```

所以不应该再围绕 `/System/Volumes/Data/Users/X` 这条命令修。它不会变成 `Indexing enabled`，因为它不是卷根。

## 最近使用为什么不出现

Finder 的“最近使用”依赖 Spotlight 元数据里的 `kMDItemLastUsedDate`。今天实测：

```sh
mdls -name kMDItemLastUsedDate '6.1.为啥mac的最近.md'
```

## 问题现象
一开始是：

- Finder 的"最近使用"里全是很久以前的文件，最近打开的文件不出现
- `mdutil -s /Users/X` 报 `Error: unknown indexing state`
- Typora、VSCode 打开的文件都不被追踪
```sh
kMDItemLastUsedDate = (null)
```

## 根本原因
这表示文件没有被系统记录为“最近使用”，所以 Finder 最近使用里不会出现。

macOS Catalina 之后系统采用了分卷架构：
用 LaunchServices 方式打开后：

| 卷宗 | 作用 |
|------|------|
| `/` (Macintosh HD) | 只读系统卷，存放 macOS 本身 |
| `/System/Volumes/Data` | 数据卷，用户文件实际存放位置 |
```sh
open -a Typora '6.1.为啥mac的最近.md'
open -a 'Visual Studio Code' 'claude配置6.1.md'
```

实测变成：

```sh
6.1.为啥mac的最近.md  -> kMDItemLastUsedDate = 2026-06-01 03:08:48 +0000
claude配置6.1.md       -> kMDItemLastUsedDate = 2026-06-01 03:09:02 +0000
```

`/Users/X` 是 `/System/Volumes/Data/Users/X` 的 firmlink 映射。Spotlight 索引按卷宗管理——如果只对 `/` 重建索引，根本不会触及用户数据所在的 Data 卷宗。
也就是说，Typora 和 VSCode 不是完全不能被追踪；关键是打开方式。通过 `open -a ... 文件` 这种系统打开方式会写入系统级最近使用记录。编辑器内部历史、命令行读文件、部分命令行打开方式可能不会触发这个记录。

## Spotlight 搜 VSCode

命令行实测 Spotlight 能找到 VSCode：

```sh
mdfind 'kMDItemFSName == "Visual Studio Code.app"c'
```

Data 卷宗的 Spotlight 索引在某个时间点损坏了（可能是异常关机、磁盘空间不足、或系统更新导致），之后就一直处于 "unknown indexing state"，不再追踪文件访问记录。
结果：

## 第一次修复为什么没用
```sh
/Applications/Visual Studio Code.app
```

第一次只对 `/`（系统卷）做了重建，但用户文件住在 `/System/Volumes/Data`（数据卷），等于修错了地方。
已执行温和修复：

## 正确的修复步骤
```sh
/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user
mdimport /Applications/'Visual Studio Code.app'
killall Spotlight 2>/dev/null || true
killall sharedfilelistd 2>/dev/null || true
```

```bash
# 1. 关闭 Data 卷宗索引
sudo mdutil -i off /System/Volumes/Data
这会重建应用注册和刷新 Spotlight 前台缓存，不会删除 Spotlight 索引。

# 2. 擦除并重建索引（通过系统 API，绕过 SIP）
sudo mdutil -E /System/Volumes/Data
## 正确验证命令

# 3. 重新开启索引
sudo mdutil -i on /System/Volumes/Data
查卷级 Spotlight 是否正常：

# 4. 确认状态（应显示 Indexing enabled）
```sh
mdutil -s /System/Volumes/Data
```

注意：不能用 `rm -rf /.Spotlight-V100` 手动删索引文件，SIP 会阻止。`mdutil -E` 是正确的擦除方式。
查某个目录里 Spotlight 能不能搜文件：

```sh
mdfind -onlyin /Users/X 'kMDItemFSName == "*.md"cd' | head
```

查某个文件有没有进入“最近使用”：

```sh
mdls -name kMDItemLastUsedDate -name kMDItemUseCount 文件.md
```

查最近使用里是否能搜到某个文件：

## 验证方法
```sh
mdfind -onlyin /Users/X 'kMDItemFSName == "文件.md"c && kMDItemLastUsedDate == *'
```

```bash
# 检查索引重建进度（重建中返回 0，完成后返回文件数）
mdfind -onlyin /Users/X -name ".md" | wc -l
## 不建议再做的事

# 检查卷宗状态
mdutil -s /System/Volumes/Data
不要再执行这类操作：

```sh
sudo rm -rf /.Spotlight-V100
sudo mdutil -E /System/Volumes/Data
```

## 关于 `mdutil -s /Users/X` 仍显示 unknown
除非已经确认卷级索引真的坏了。当前 Data 卷索引是 enabled，`mdfind` 也能搜到文件和应用，清索引只会让 Spotlight 暂时更乱、更慢。

这是正常的——`/Users/X` 不是独立卷宗，`mdutil` 对子目录查询会报这个错。只要父卷宗 `/System/Volumes/Data` 状态为 `Indexing enabled` 就没问题。
也不要把 `mdutil -s /Users/X` 或 `mdutil -s /System/Volumes/Data/Users/X` 当成修复目标。这两条报 `unknown indexing state` 是预期结果。

## 实用用法

如果想让某个 md 文件进入 Finder 最近使用，用：

## 时间线
```sh
open -a Typora 文件.md
open -a 'Visual Studio Code' 文件.md
```

- 2026-06-01 10:10 — 首次尝试对 `/` 重建，无效
- 2026-06-01 10:13 — 对 `/System/Volumes/Data` 重建，成功
- 重建完成需 10-30 分钟，之后"最近使用"恢复正常
不要只用 `cat`、`sed`、编辑器内部历史，或者其他没有经过系统打开服务的方式来判断 Finder 最近使用是否正常。