# macOS Finder「最近使用」为空的真实排障总结

日期：2026-06-01

目标读者：后续接手的 AI / 人类维护者

这份文档是为了避免后续继续误判问题、乱清缓存、乱重建 Spotlight，尤其是不要再把 `mdutil -s /System/Volumes/Data/Users/X` 当成必须修成 `Indexing enabled` 的目标。

## 一句话结论

Finder 侧边栏里的系统内置「最近使用」为空，不是因为 Spotlight 完全坏了，也不是因为 `/System/Volumes/Data` 卷没有索引。

实测底层最近使用数据存在，Spotlight 能查到大量带 `kMDItemLastUsedDate` 的文件。

真正可用的修复是创建一个自定义 saved search：

```text
/Users/bergman/Library/Saved Searches/最近使用-含X.savedSearch
```

这个智能文件夹明确把搜索范围设成 `/Users`，因此能包含 `/Users/X` 里的工作文件。用户已确认这个窗口显示正确。

## 机器上的关键路径关系

当前 shell 环境：

```sh
whoami
# bergman

echo "$HOME"
# /Users/bergman
```

但是用户大量工作文件在：

```text
/Users/X
```

也就是说：

```text
登录用户 home: /Users/bergman
主要工作目录: /Users/X
```

这非常关键。Finder 内置「最近使用」使用的是系统定义的“我的文件”范围，不等价于简单扫描整个 `/Users`。在这种 home 和工作区分离的结构下，Finder 内置 Recents 很容易范围不对、漏掉 `/Users/X`，甚至直接显示为空。

## 关于 `mdutil -s /System/Volumes/Data/Users/X`

用户反复看到：

```sh
mdutil -s /System/Volumes/Data/Users/X
```

输出：

```text
/System/Volumes/Data/Users/X:
	Error: unknown indexing state.
```

这条输出本身不能当成“Spotlight 坏了”的证明。

原因：`mdutil` 管的是 Spotlight 的“卷”级索引状态，不是普通目录的索引状态。`/System/Volumes/Data/Users/X` 是目录，不是卷根。

真正应该看的卷是：

```sh
mdutil -s /System/Volumes/Data
```

实测结果：

```text
/System/Volumes/Data:
	Indexing enabled.
```

所以，`mdutil -s /System/Volumes/Data/Users/X` 报 `unknown indexing state` 是一个容易误导人的表象。它不应该成为修复目标。

不要试图把这条命令修成 `Indexing enabled`。方向错了。

## 真实数据层验证

Finder 内置「最近使用」的核心条件来自系统自带 saved search，近似为：

```text
(kMDItemLastUsedDate = "*") &&
(
  (kMDItemContentTypeTree = public.content) ||
  (kMDItemContentTypeTree = "com.microsoft.*"cdw) ||
  (kMDItemContentTypeTree = public.archive)
)
```

用命令行按类似条件查，底层数据不是空。

实测：

```sh
mdfind 'kMDItemLastUsedDate == *' | head
```

能返回文件和应用，数量约几百条。

更接近 Finder Recents 的查询：

```sh
q='(kMDItemLastUsedDate == *) && ((kMDItemContentTypeTree == public.content) || (kMDItemContentTypeTree == "com.microsoft.*"cdw) || (kMDItemContentTypeTree == public.archive))'
mdfind -onlyin /Users "$q" | head
```

实测能返回大量结果，包括：

```text
/Users/X/AI/daydayai/claude配置6.1.md
/Users/X/AI/daydayai/6.1.为啥mac的最近.md
/Users/X/AI/daydayai/键盘/键帽.md
/Users/X/keydog/doc/22ing/功能更新记录.md
...
```

实测数量约：

```text
expected_count=528
```

所以底层 Spotlight 最近使用元数据不是空的。

## 文件最近使用元数据验证

Finder「最近使用」主要看 `kMDItemLastUsedDate`。

例如一开始某些文件没有最近使用记录：

```sh
mdls -name kMDItemLastUsedDate '6.1.为啥mac的最近.md'
```

可能是：

```text
kMDItemLastUsedDate = (null)
```

这表示该文件没有被系统级最近使用记录捕获。

通过 LaunchServices 打开后：

```sh
open -a Typora '6.1.为啥mac的最近.md'
open -a 'Visual Studio Code' 'claude配置6.1.md'
```

实测变成：

```text
6.1.为啥mac的最近.md  -> kMDItemLastUsedDate = 2026-06-01 03:08:48 +0000
claude配置6.1.md       -> kMDItemLastUsedDate = 2026-06-01 03:09:02 +0000
```

说明 Typora 和 VSCode 并非完全不会写入最近使用记录。关键是打开路径和系统是否通过 LaunchServices 记录到了这次打开。

注意：编辑器内部历史、命令行读文件、某些非标准打开方式，不一定会触发 Finder 最近使用需要的 `kMDItemLastUsedDate`。

## 为什么 Finder 内置「最近使用」还是空

目前最合理判断：

1. Spotlight 卷级索引是开的。
2. `kMDItemLastUsedDate` 数据存在。
3. 按 Finder Recents 类似条件在命令行能查到数百条。
4. 但 Finder 侧边栏内置「最近使用」显示为空。
5. 用户工作文件主要位于 `/Users/X`，而登录用户 home 是 `/Users/bergman`。
6. Finder 内置 Recents 使用 `kMDQueryScopeMyFiles` 之类的系统“我的文件”范围，不等于 `/Users`。

所以问题很可能在 Finder 内置 Recents 的查询范围或视图层，而不是 Spotlight 数据层。

简单说：

```text
数据有。
Finder 内置入口没正确显示。
明确范围为 /Users 的自定义 saved search 能正确显示。
```

## 已执行过的操作

### 1. 验证 Data 卷 Spotlight 状态

```sh
mdutil -s /System/Volumes/Data
```

结果：

```text
/System/Volumes/Data:
	Indexing enabled.
```

### 2. 验证 VSCode 能被 Spotlight 找到

```sh
mdfind 'kMDItemFSName == "Visual Studio Code.app"c'
```

结果：

```text
/Applications/Visual Studio Code.app
```

### 3. 温和刷新 LaunchServices / Spotlight 前台缓存

执行过：

```sh
/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user
mdimport /Applications/'Visual Studio Code.app'
killall Spotlight 2>/dev/null || true
killall sharedfilelistd 2>/dev/null || true
```

这个操作没有删除 Spotlight 索引，只是重建应用注册和刷新部分前台缓存。

用户反馈：VSCode 已经能被 Spotlight 搜到了，并且看到系统在重建/刷新索引。

### 4. 重置过 Finder 的部分 Recents/Search 视图偏好

备份过 Finder 偏好到 `/tmp`：

```text
/tmp/com.apple.finder.plist.backup.20260601-111657
```

删除过以下 Finder 偏好键：

```text
SearchRecentsViewSettings
SearchRecentsSavedViewStyle
SearchRecentsSavedViewStyleVersion
RecentsArrangeGroupViewBy
SmartSharedSearchViewSettings
FXPreferredSearchViewStyle
FXPreferredSearchViewStyleVersion
```

然后执行过：

```sh
killall Finder
open /System/Library/CoreServices/Finder.app/Contents/Applications/Recents.app
```

这一步是 Finder 层修复尝试，不是 Spotlight 重建。

### 5. 创建了可用的自定义 saved search

文件：

```text
/Users/bergman/Library/Saved Searches/最近使用-含X.savedSearch
```

其核心查询条件与 Finder 原始 Recents 类似，但 SearchScopes 明确设为：

```text
/Users
```

验证：

```sh
plutil -lint "$HOME/Library/Saved Searches/最近使用-含X.savedSearch"
```

结果：

```text
OK
```

打开：

```sh
open "$HOME/Library/Saved Searches/最近使用-含X.savedSearch"
```

用户确认：这个窗口是正确的。

## 当前有效修复方案

当前实际可用方案是使用：

```text
/Users/bergman/Library/Saved Searches/最近使用-含X.savedSearch
```

它是一个 Finder 智能文件夹。

建议用户把它拖到 Finder 侧边栏，作为替代系统内置「最近使用」的入口。

这不是“绕过问题假装修好”，而是针对当前机器目录结构的正确修复：明确告诉 Finder 搜 `/Users`，不要依赖系统内置的 `kMDQueryScopeMyFiles`。

## 之前 AI 可能做错了什么

根据现象推断，之前 AI 大概率犯了这些错误：

### 错误 1：把目录级 `mdutil` 报错当成核心故障

它盯着：

```sh
mdutil -s /System/Volumes/Data/Users/X
```

但这个路径不是卷根，报 `unknown indexing state` 并不等于 Data 卷索引坏。

### 错误 2：清理或重建了过多 Spotlight / 最近项目相关缓存

用户反馈：

```text
历史记录都被清空了
Spotlight 连 VSCode 都搜不到了
```

这说明之前很可能动了 Spotlight 索引、LaunchServices、SharedFileList 或 Finder 最近项目相关数据，而且没有先做足够验证。

这种操作会造成短期内：

```text
应用搜不到
最近项目消失
Spotlight 后台大量重建
Finder 结果混乱
```

### 错误 3：声称已经修好，但没有验证 Finder UI

真正的问题是 Finder 侧边栏「最近使用」为空。

只验证：

```sh
mdutil -s /
mdutil -s /System/Volumes/Data
mdfind ...
```

不等于 Finder UI 修好了。

必须验证：

1. Finder 内置 Recents 是否显示。
2. 或自定义 saved search 是否显示。
3. 用户实际打开 Finder 后是否能看到结果。

这次最终是用户确认 `最近使用-含X.savedSearch` 窗口正确，才算找到了可用解法。

## 后续不要再做的事

不要再执行这类破坏性/大范围操作，除非有新的明确证据：

```sh
sudo rm -rf /.Spotlight-V100
sudo rm -rf /System/Volumes/Data/.Spotlight-V100
sudo mdutil -E /
sudo mdutil -E /System/Volumes/Data
sudo mdutil -i off /
sudo mdutil -i off /System/Volumes/Data
```

原因：

1. 当前 Data 卷索引已经是 `Indexing enabled`。
2. `mdfind` 能查到最近使用数据。
3. 大范围重建只会让 Spotlight 暂时更乱、更慢。
4. 用户已经被清过历史记录，很烦。

也不要继续承诺：

```text
我会把 mdutil -s /System/Volumes/Data/Users/X 修成 Indexing enabled
```

这是错误目标。

## 后续如果还要继续修内置 Finder Recents

如果一定要继续研究系统内置「最近使用」为什么空，可以从这些方向查：

1. Finder 内置 Recents.app：

   ```text
   /System/Library/CoreServices/Finder.app/Contents/Applications/Recents.app
   ```

2. Finder 自带 My Documents saved search：

   ```text
   /System/Library/CoreServices/Finder.app/Contents/Resources/MyLibraries/myDocuments.cannedSearch/Resources/search.savedSearch
   ```

3. 系统 saved search 中的 scope：

   ```text
   kMDQueryScopeMyFiles
   ```

4. 当前用户 home 和工作目录分离：

   ```text
   /Users/bergman
   /Users/X
   ```

5. Finder 偏好：

   ```text
   ~/Library/Preferences/com.apple.finder.plist
   ```

但是，继续修内置 Recents 的收益不一定高。因为自定义 saved search 已经能稳定解决“用户要在 Finder 看到最近使用文件”的真实需求。

## 给后续 AI 的工作原则

请不要先解释一大堆 macOS 卷结构，然后结束。

用户真正要的是：

```text
Finder 里的最近使用不要是空的。
打开过的文件要能出现在一个 Finder 窗口里。
```

当前已经证明：

```text
最近使用-含X.savedSearch 是正确的。
```

如果继续处理，优先做这些低风险事情：

1. 帮用户把 `最近使用-含X.savedSearch` 固定到 Finder 侧边栏。
2. 保留这个 saved search 文件。
3. 不要清 Spotlight。
4. 不要重建全盘索引。
5. 不要删除用户历史记录。
6. 不要把 `mdutil -s /System/Volumes/Data/Users/X` 当成终点。

## 最短版

```text
内置 Finder 最近使用为空。
Spotlight 数据不空。
Data 卷索引是 enabled。
目录级 mdutil unknown indexing state 不是核心故障。
登录 home 是 /Users/bergman，工作区在 /Users/X。
Finder 内置 Recents 的“我的文件”范围可能漏掉 /Users/X。
创建 /Users/bergman/Library/Saved Searches/最近使用-含X.savedSearch 后，用户确认窗口正确。
后续不要清索引，优先把这个 saved search 固定到侧边栏。
```
