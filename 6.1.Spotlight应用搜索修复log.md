
# 7.22
spotlight是出啥事了? 好奇怪啊, terminal搜索不到, 为啥? 你之前帮我处理过的.
所以要咋办?
所以核心问题是, 我在外置硬盘上面安装macos26引起的?

## 2026-07-22 系统应用索引脱节

Symptom: Spotlight 搜索不到启动卷上的系统应用。
Case: 搜索 Terminal、System Settings 或 TextEdit。
Expected: Spotlight 返回 `/System/Applications` 中对应的应用。
Actual: 启动卷结果缺失，Terminal 只返回外接 Tahoe-Test 卷中的副本。
Acceptance: `zsh bugfix-tests/spotlight-system-app-search.sh`


# 7.19
再次发生, spotlight几乎搜索不到所有app, 包括:
Chrome
vs code
keydog

key可以搜索到钥匙串, 但是无法搜索到keynotes.app
你看下之前一个多月的的修复记录: [text](6.1.Spotlight应用搜索修复log.md)

## 2026-07-19 复发记录

Symptom: Spotlight 几乎搜索不到已安装应用。
Case: 搜索 Chrome、Visual Studio Code、Keynote 或 keydog。
Expected: Spotlight 返回对应的已安装应用。
Actual: Chrome、Visual Studio Code、keydog 均无结果；搜索 key 只返回钥匙串，不返回 Keynote。
Acceptance: `zsh bugfix-tests/spotlight-app-search.sh`

已修复。

这次和 6 月不同：不是 LaunchServices 缓存问题，而是 Data 卷的 Spotlight 索引 store 卡死。普通 `mdimport`、重建 LaunchServices 均无效。

已执行：

- 清除旧索引 store，共移除 626 个文件/目录
- 重启 `mds` / `mds_stores`
- 重新启用并重建根卷索引
- 刷新 Spotlight 前台缓存

验证通过：

- `key` → Keynote、Keydog
- `Chrome` → Google Chrome
- `Visual Studio Code` → VS Code
- `keydog` → Keydog
- 应用索引数量从 281 增至 315，后台仍会继续补全

复发记录和验收命令已写入 [6.1.Spotlight应用搜索修复log.md](/Users/x/AI算法_git/daydayai/6.1.Spotlight应用搜索修复log.md:11)，回归测试在 [spotlight-app-search.sh](/Users/x/AI算法_git/daydayai/bugfix-tests/spotlight-app-search.sh:1)，执行结果为通过。管理员密码未写入任何文件。





# Spotlight 应用搜索修复 log

日期：2026-06-01

## 初始现象

- Spotlight 里搜不到 VSCode。
- 用户反馈“现在 Spotlight 连 VSCode 都搜索不到了”。
- 这很可能发生在之前清理/重建 Spotlight 或 LaunchServices 后，应用注册和 Spotlight 前台缓存处于混乱状态。

## 关键验证

命令行验证 VSCode 应用是否在 Spotlight 元数据里：

```sh
mdfind 'kMDItemFSName == "Visual Studio Code.app"c'
```

修复后结果：

```sh
/Applications/Visual Studio Code.app
```

检查 VSCode 元数据：

```sh
mdls -name kMDItemFSName -name kMDItemContentType -name kMDItemLastUsedDate /Applications/'Visual Studio Code.app'
```

结果：

```sh
kMDItemContentType  = "com.apple.application-bundle"
kMDItemFSName       = "Visual Studio Code.app"
kMDItemLastUsedDate = 2026-06-01 03:09:02 +0000
```

说明 VSCode 已经被 Spotlight 识别为应用 bundle。

## 修复动作

### 1. 重建 LaunchServices 应用注册

执行：

```sh
/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user
```

作用：

- 重建系统、用户、本地 domain 的应用注册。
- 解决应用存在于 `/Applications`，但 Spotlight/系统打开服务前台搜索不到的问题。

### 2. 重新导入 VSCode 应用元数据

执行：

```sh
mdimport /Applications/'Visual Studio Code.app'
```

作用：

- 让 Spotlight 重新读取 VSCode.app 的元数据。
- 确认它作为 `com.apple.application-bundle` 被索引。

### 3. 重启 Spotlight 前台和 sharedfilelistd

执行过：

```sh
killall Spotlight 2>/dev/null || true
killall sharedfilelistd 2>/dev/null || true
```

作用：

- 刷新 Spotlight 菜单/前台搜索进程。
- 刷新最近项目/共享文件列表相关守护进程。

注意：

- 这个操作不等于删除 Spotlight 索引。
- 它只是让相关用户态进程重启。

## 结果

修复后：

```sh
mdfind 'kMDItemFSName == "Visual Studio Code.app"c'
```

能返回：

```sh
/Applications/Visual Studio Code.app
```

用户也反馈：

```text
vscode能搜索到了
```

## 当前判断

VSCode 搜不到的根因更像是：

1. 之前的操作重建/清理了 Spotlight 或相关缓存。
2. LaunchServices 应用注册暂时不完整。
3. Spotlight 前台搜索缓存没有及时刷新。

这次真正有效的操作是：

```sh
lsregister -kill -r -domain local -domain system -domain user
mdimport /Applications/'Visual Studio Code.app'
killall Spotlight
```

## 注意事项

如果以后只是某个 app 搜不到，优先做这种小范围修复：

```sh
/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user
mdimport /Applications/'AppName.app'
killall Spotlight 2>/dev/null || true
```

不要一上来清全盘 Spotlight 索引。
