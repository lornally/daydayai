# Spotlight 应用搜索修复 log

日期：2026-06-01

## 初始现象

- Spotlight 里搜不到 VSCode。
- 用户反馈“现在 Spotlight 连 VSCode 都搜索不到了”。
- 这很可能发生在之前清理/重建 Spotlight 或 LaunchServices 后，应用注册和 Spotlight 前台缓存处于混乱状态。

## 关键验证

命令行验证 VSCode 应用是否在 Spotlight 元数据里：

```sh
mdfind 'kMDItemFSName == "Visual Studio Code.app"c'
```

修复后结果：

```sh
/Applications/Visual Studio Code.app
```

检查 VSCode 元数据：

```sh
mdls -name kMDItemFSName -name kMDItemContentType -name kMDItemLastUsedDate /Applications/'Visual Studio Code.app'
```

结果：

```sh
kMDItemContentType  = "com.apple.application-bundle"
kMDItemFSName       = "Visual Studio Code.app"
kMDItemLastUsedDate = 2026-06-01 03:09:02 +0000
```

说明 VSCode 已经被 Spotlight 识别为应用 bundle。

## 修复动作

### 1. 重建 LaunchServices 应用注册

执行：

```sh
/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user
```

作用：

- 重建系统、用户、本地 domain 的应用注册。
- 解决应用存在于 `/Applications`，但 Spotlight/系统打开服务前台搜索不到的问题。

### 2. 重新导入 VSCode 应用元数据

执行：

```sh
mdimport /Applications/'Visual Studio Code.app'
```

作用：

- 让 Spotlight 重新读取 VSCode.app 的元数据。
- 确认它作为 `com.apple.application-bundle` 被索引。

### 3. 重启 Spotlight 前台和 sharedfilelistd

执行过：

```sh
killall Spotlight 2>/dev/null || true
killall sharedfilelistd 2>/dev/null || true
```

作用：

- 刷新 Spotlight 菜单/前台搜索进程。
- 刷新最近项目/共享文件列表相关守护进程。

注意：

- 这个操作不等于删除 Spotlight 索引。
- 它只是让相关用户态进程重启。

## 结果

修复后：

```sh
mdfind 'kMDItemFSName == "Visual Studio Code.app"c'
```

能返回：

```sh
/Applications/Visual Studio Code.app
```

用户也反馈：

```text
vscode能搜索到了
```

## 当前判断

VSCode 搜不到的根因更像是：

1. 之前的操作重建/清理了 Spotlight 或相关缓存。
2. LaunchServices 应用注册暂时不完整。
3. Spotlight 前台搜索缓存没有及时刷新。

这次真正有效的操作是：

```sh
lsregister -kill -r -domain local -domain system -domain user
mdimport /Applications/'Visual Studio Code.app'
killall Spotlight
```

## 注意事项

如果以后只是某个 app 搜不到，优先做这种小范围修复：

```sh
/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user
mdimport /Applications/'AppName.app'
killall Spotlight 2>/dev/null || true
```

不要一上来清全盘 Spotlight 索引。

