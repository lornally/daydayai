# Finder 最近使用修复 log

日期：2026-06-01

## 初始现象

- Finder 侧边栏里的「最近使用」为空。
- 用户打开 Typora、VSCode 文件后，Finder「最近使用」仍然不出现新文件。
- `mdutil -s /System/Volumes/Data/Users/X` 报：

```sh
/System/Volumes/Data/Users/X:
	Error: unknown indexing state.
```

## 关键判断

`mdutil -s /System/Volumes/Data/Users/X` 不是核心故障点。

真正需要看的卷级状态是：

```sh
mdutil -s /System/Volumes/Data
```

实测：

```sh
/System/Volumes/Data:
	Indexing enabled.
```

所以 Data 卷 Spotlight 索引是开启的。

## 做过的关键验证

检查底层最近使用元数据：

```sh
mdfind 'kMDItemLastUsedDate == *' | wc -l
```

后来实测已经有大量结果：

```sh
7759
```

检查单个文件是否有最近使用记录：

```sh
mdls -name kMDItemLastUsedDate -name kMDItemUseCount '6.1.为啥mac的最近.md'
mdls -name kMDItemLastUsedDate -name kMDItemUseCount 'claude配置6.1.md'
```

结果：

```sh
kMDItemLastUsedDate = 2026-06-01 03:08:48 +0000
kMDItemUseCount     = 21

kMDItemLastUsedDate = 2026-06-01 03:09:02 +0000
kMDItemUseCount     = 2
```

说明底层最近使用数据已经存在。

## 做过的修复动作

### 1. 用 LaunchServices 打开文件，触发最近使用记录

```sh
open -a Typora '6.1.为啥mac的最近.md'
open -a 'Visual Studio Code' 'claude配置6.1.md'
```

作用：

- 让 macOS 通过系统打开服务记录文件使用行为。
- 触发/验证 `kMDItemLastUsedDate`。

### 2. 重置 Finder 的 Recents/Search 视图缓存

先备份 Finder 偏好：

```sh
cp ~/Library/Preferences/com.apple.finder.plist /tmp/com.apple.finder.plist.backup.20260601-111657
```

删除过这些 Finder 视图缓存键：

```sh
defaults delete com.apple.finder SearchRecentsViewSettings
defaults delete com.apple.finder SearchRecentsSavedViewStyle
defaults delete com.apple.finder SearchRecentsSavedViewStyleVersion
defaults delete com.apple.finder RecentsArrangeGroupViewBy
defaults delete com.apple.finder SmartSharedSearchViewSettings
defaults delete com.apple.finder FXPreferredSearchViewStyle
defaults delete com.apple.finder FXPreferredSearchViewStyleVersion
```

然后重启 Finder：

```sh
killall Finder
```

作用：

- 让 Finder 重新生成/读取最近使用窗口的视图配置。
- 避免 Finder 用坏掉的旧 Recents 视图缓存继续显示空白。

### 3. 用正确方式打开原生 Recents

后来发现直接 `open /System/.../Recents.app` 有时窗口不在前台，容易误判。

更准确的打开方式是：

```sh
open -b com.apple.finder.Open-Recents
```

这个 bundle id 来自：

```sh
/System/Library/CoreServices/Finder.app/Contents/Applications/Recents.app
```

其 `CFBundleIdentifier` 是：

```sh
com.apple.finder.Open-Recents
```

## 当前判断

不能确认是哪一个命令单独修好了 Finder「最近使用」。

更可能是组合效果：

1. 之前 Spotlight / SharedFileList 被清理或重建后，后台索引尚未恢复完成。
2. Spotlight 后台重建逐渐完成，`kMDItemLastUsedDate` 数据重新变多。
3. 用 `open -a ...` 触发了文件的系统级最近使用记录。
4. Finder Recents 视图缓存被重置。
5. 重启电脑后，Finder、Spotlight、sharedfilelistd 状态重新加载。

最终结果：

- Finder 原生「最近使用」从空白恢复为有内容。
- `mdfind 'kMDItemLastUsedDate == *'` 已经能返回大量结果。

## 排除项

`FXDefaultSearchScope` 不是主因。

曾经改过：

```sh
defaults write com.apple.finder FXDefaultSearchScope -string SCev
```

用户测试后认为“搜索当前”和“搜索全 Mac”没区别。

后来当前值又是：

```sh
SCcf
```

但 Finder 最近使用已经有内容，所以它不是这次问题的核心。

## 注意事项

不要再为了这个问题清空 Spotlight 索引。

不建议执行：

```sh
sudo rm -rf /.Spotlight-V100
sudo rm -rf /System/Volumes/Data/.Spotlight-V100
sudo mdutil -E /
sudo mdutil -E /System/Volumes/Data
```

除非有新的证据证明 Data 卷索引真的坏了。

